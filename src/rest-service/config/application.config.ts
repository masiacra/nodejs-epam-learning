export const PORT = 4000;
export const DEFAULT_LIMIT = 10;

export const INTERNAL_PROBLEM = new Error('Sorry. Some propblems with server');

export const BAD_LOGIN_OR_PASSWORD = new Error('Bad login or password');

export const NO_TOKEN_PROVIDED = new Error('No token provided!');

export const FAILED_TO_AUTHENTICATE = new Error('Failed to authenticate!');
